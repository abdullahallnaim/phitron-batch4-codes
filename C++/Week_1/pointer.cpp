#include <bits/stdc++.h>
using namespace std;

int main()
{
    // int a = 10; // stack er moddhe save hocche
    // int *p;     // address variable amra pointer name chini
    // p = &a;     // a er address hold korteche, reference kora
    // *p = 100;   // derefence
    // cout << a << endl;
    // int b = &a;
    // cout << *b << endl;

    //
    // dynamic array
    // int a[5] = {1, 2, 3, 4, 5};
    // int *p;
    // // p = a; // a er first value address ta thakbe
    // p = &a[0]; 
    // cout << p << endl;
    // int *arr = new int[10];
    // arr[0] = 10;
    // arr[1] = 200;
    // lasdjflaskd
    // delete[] arr;
    // delete arr;
    // free(arr);
    int a = 10, b = 0;
    cout << "Print Successfull";
    cout << a / b << endl; // stop hoye ache
    return 0;
}
